name: Codeatlas
on:
  push:
    branches:
      - main
jobs:
  codeatlas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: codeatlasHQ/codebase-visualizer-action@v1-beta
